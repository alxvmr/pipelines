version: '3.8'
services:
  pipelines:
    build: ./
    #command: bash -c 'while !</dev/tcp/db/5432; do sleep 1; done;'
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_DB=pipelines
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=12345
    volumes:
      - ./example_pipeline/original/original.csv:/pipelines/example_pipeline/original/original.csv
  db:
    restart: always
    build: ./db/
    ports:
      - "5432:5432"
    healthcheck:
      test : ["CMD-SHELL", "pg_isready --username=postgres -p 5432"]
      interval: 10s
      timeout: 5s
      retries: 5
#volumes:
#  original:
#    driver: local
#    driver_opts:
#      o: bind
#      type: none
#      device: ./example_pipeline/original/original.csv